import { Text } from '@mantine/core';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Open source Gig/Job Platform - Contact',
};

export default function ContactPage() {
  return <Text>This is contact page</Text>;
}
