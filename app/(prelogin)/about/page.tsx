import { Text } from '@mantine/core';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Open source Gig/Job Platform - About',
};

export default function AboutPage() {
  return <Text>This is about page</Text>;
}
